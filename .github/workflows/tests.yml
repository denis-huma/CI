name: Tests

#on: push
on: workflow_dispatch

jobs:
    build:

    runs-on: macos-latest

    steps:
        - name: Checkout repo for tests
        - uses: actions/checkout@v3
        run: swift build

        - name: Run tests
        run: swift test
